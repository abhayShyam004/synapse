# Task 1: Project Setup & Dependencies Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Correct the project setup by installing missing Tailwind dependencies and adding the necessary directives to the CSS entry point.

**Architecture:** Standard Tailwind CSS integration for a Vite/React/TypeScript project.

**Tech Stack:** React, TypeScript, Tailwind CSS, PostCSS, Autoprefixer, npm.

---

### Task 1: Install Tailwind CSS Engine and Peers

**Files:**
- Modify: `package.json` (via `npm install`)

- [ ] **Step 1: Install missing dependencies**

Run: `npm install -D tailwindcss postcss autoprefixer`
Expected: Dependencies added to `package.json` and `node_modules`.

- [ ] **Step 2: Verify installation**

Check `package.json` for:
```json
"devDependencies": {
  ...
  "autoprefixer": "^10.4.20",
  "postcss": "^8.4.49",
  "tailwindcss": "^3.4.17",
  ...
}
```

- [ ] **Step 3: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: install tailwindcss, postcss, and autoprefixer"
```

### Task 2: Add Tailwind Directives to CSS Entry Point

**Files:**
- Modify: `src/index.css`

- [ ] **Step 1: Add @tailwind directives to the top of the file**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  ...
}
```

- [ ] **Step 2: Commit**

```bash
git add src/index.css
git commit -m "feat: add tailwind directives to index.css"
```

### Task 3: Final Verification

- [ ] **Step 1: Run build to ensure Tailwind processing works**

Run: `npm run build`
Expected: Build succeeds without CSS processing errors.

- [ ] **Step 2: Check for Tailwind-generated classes in output (Optional but recommended)**

---
